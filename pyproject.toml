[project]
name = "terminal-naf"
version = "2.0.0"
description = "Terminal de triagem, fila e atendimento do NAF"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
    "Flask>=3.0,<4.0",
]

[tool.vercel]
entrypoint = "index:app"
