# Arquivos reconstruídos

Os arquivos `index.html`, `atendimento.html` e `informacoes.html` eram referenciados pelo `app.py` e pela estrutura descrita no README, mas não estavam entre os arquivos enviados.

Foram criadas versões mínimas compatíveis com as rotas, variáveis Jinja e classes CSS já existentes, apenas para deixar o protótipo executável.

Se os arquivos originais forem enviados depois, basta substituí-los em `templates/`.
